{
  "name": "oasis-guest-house-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'Frontend build complete (static files)'",
    "install": "echo 'No dependencies to install'"
  }
}